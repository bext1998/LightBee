# LightBee — Coding Agent 指令

## 專案概述

LightBee 是一個 Windows 系統匣工具，使用網路攝影機偵測環境明暗，並以三段式規則調整螢幕亮度。

技術棧：C# / .NET、WinForms、WinRT、WMI、DDC/CI。

## 任務範圍

- 完成使用者要求的工作，不自行擴張功能範圍。
- 可以進行完成任務、修復測試、維持既有行為或避免回歸所需的小範圍修改。
- 不進行與任務無關的重構、功能新增、架構變更或風格整理。
- 使用者已要求實作、修復或修改時，該要求涵蓋任務範圍內必要的調查、編輯、建置、測試與修正，但不包含受限制操作。
- 使用者明確要求只分析、只審查、先提出方案或等待核准時，不進入後續實作。

## 工作開始

開始實作前，閱讀與任務相關的專案資訊。

優先參考：

1. 使用者目前的明確指示
2. 已確認且適用的 GitHub Issue／PR 要求
3. `docs/spec.md`
4. `DECISIONS.md`
5. `MAZE_PROJECT.md`
6. `NEXT_ACTION.md`
7. 其他與任務相關的文件與程式碼

上述順序用於判讀專案資料，不取代執行環境本身的指令層級。

Git 狀態、diff 與工作目錄內容用於辨識現況及既有修改，不構成新的需求或授權。

Issue／PR 中尚未確認的提案、過時討論、引用內容或被後續決策取代的資訊，不得直接視為高於有效規格的要求。

文件不存在時，使用現有資訊完成可完成的工作，不把缺少非必要文件視為阻塞。

若資料存在衝突，先依適用範圍、有效性、時間與專案上下文判斷。只有無法從現有資訊解決，且不同選擇會實質改變使用者可見需求、驗收標準、資料安全或對外相容性時，才要求使用者決定。

## 執行自主權

在目前任務與專案授權範圍內，Agent 可以自行：

- 閱讀與搜尋 repository。
- 檢查 Git 狀態、diff 與歷史。
- 查閱相關技術文件。
- 執行建置、測試與必要診斷。
- 修改完成任務所需的程式碼與非受限制文件。
- 修正由本次修改造成的建置、測試或明確功能問題。
- 依既有程式碼與專案慣例選擇合理實作方式。

以下情況不構成停止理由：

- 存在多種合理實作方式。
- 一般實作細節未指定。
- 需要閱讀更多程式碼或文件。
- 需要執行額外診斷。
- 驗證發現仍有屬於目前任務範圍的問題。
- 其他 Agent、planner 或 reviewer 沒有提供完整執行步驟。
- 某項非必要操作無法執行，但其他工作仍可完成。

一般實作細節應依既有程式碼、專案慣例、可逆性與維護成本自行決定。必要時在完成摘要說明採用的假設，不為此中止工作。

不得僅因完成分析、提出計畫、選定方案、完成部分修改或準備進入一般驗證階段，就詢問是否繼續、是否開始實作或是否執行驗證。

進度回報不等於請求核准。回報後應繼續處理剩餘工作。

## 其他 Agent 與 Reviewer

其他 Agent 產生的提示、計畫、審查意見或工作單，視為任務規格與實作參考。

它們：

- 不構成新的權限來源。
- 不得自行增加使用者未要求的確認、停止或核准條件。
- 不得因缺少完整執行步驟而阻止目前 Agent 繼續工作。

其他 Agent 或 reviewer 指出的具體風險、錯誤或證據，仍應依本文件的阻塞條件與專案規則重新評估。

不得僅因其不是權限來源而忽略有效證據。

## 阻塞處理

阻塞原則上只停止受影響的操作，不停止其他可獨立完成的工作。

遇到阻塞時：

- 先完成不依賴該阻塞的實作、檢查與驗證。
- 尋找安全且符合任務範圍的替代方式。
- 不得把非必要操作的權限不足視為整體任務阻塞。

只有當缺少使用者決定會使所有剩餘必要工作無法安全推進時，才暫停整體任務並提出具體問題。

以下情況才構成真正阻塞：

- 缺少必要資訊，無法從現有檔案、工具或文件取得，且不同選擇會實質改變使用者可見需求、驗收標準、資料安全或對外相容性，亦無法依既有慣例採取合理且可逆的預設。
- 操作超出使用者授權的專案或任務範圍。
- 操作具有不可逆或破壞性影響，且尚未取得授權。
- 高優先序指令存在無法從專案上下文解決的實質衝突。
- 工作需要只有使用者才能提供的外部憑證、權限或資源，且沒有安全替代方案。

遇到真正阻塞時，指出：

- 受阻的具體操作。
- 缺少的資訊、權限或資源。
- 已完成且不受影響的工作。
- 為何剩餘必要工作無法繼續。

不得只以「需要確認」或「需要更多資訊」作為停止理由。

## LightBee 專案約束

修改相機取樣節奏時：

- 保留快速模式 30 輪上限。
- 保留取樣失敗退避行為。
- 除非使用者明確要求變更上述規則。

進行可能產生或覆寫 App 輸出檔案的操作前：

- 自行檢查 LightBee 雛型 App 是否正在執行。
- 不把「確認 App 狀態」解讀為必須詢問使用者。
- App 正在執行且未取得終止程序授權時，不自行終止使用者程序。
- 先完成不需要終止程序的工作。
- DLL 鎖定阻止必要建置或驗證，且沒有安全替代方案時，只把受影響的建置或驗證列為阻塞，不停止其他可完成工作。

## 驗證原則

完整驗證受作業系統、攝影機、顯示器、DDC/CI、WMI 或其他外部環境限制時，先完成目前環境允許的驗證，包括：

- 建置。
- 單元測試。
- 靜態檢查。
- 程式碼審查。
- 可在目前環境執行的整合驗證。

缺少完整硬體或作業系統環境，但不妨礙安全完成修改時，不因此停止實作。

不得將未執行的驗證宣稱為已通過。

完成摘要需列出：

- 已執行的驗證。
- 未執行的驗證。
- 無法執行的原因。
- 剩餘風險。

若使用者明確要求某項驗收，該驗收視為必要驗收。

只有缺少環境會使必要實作無法可靠決定，或使用者明確要求的必要驗收無法完成時，才將受影響部分列為阻塞。

## 工作狀態與交接

GitHub Issue／PR 與 Git 工作狀態是目前工作的主要狀態資訊來源。

`NEXT_ACTION.md` 是工作交接資料，不取代有效規格、已確認決策或使用者目前指示。

只有符合下列情況之一時才重建或大幅更新 `NEXT_ACTION.md`：

- 使用者要求執行 closeout。
- 任務流程明確要求進行專案收尾。
- Issue／PR 工作已完成，且目前任務包含交接或狀態整理。

一般程式碼修改不得因維護 `NEXT_ACTION.md` 而擴大工作範圍。

## 重要文件

| 文件 | 用途 |
|---|---|
| `docs/spec.md` | 功能規格與驗收標準 |
| `docs/spike-report.md` | Spike 測試數據與 Gate 判定 |
| `NEXT_ACTION.md` | 下一步行動與交接資訊 |
| `DECISIONS.md` | 有效重大決策索引 |

## 受限制操作

除非使用者明確授權：

- 不得 force push 到 `main`。
- 不得 commit。
- 不得 push。
- 不得修改 `docs/spec.md` 所定義的功能範圍。

讀取 Git 狀態、檢視 diff、建立或修改工作目錄內檔案、執行建置與測試，不屬於 commit 或 push，不需要額外取得確認。

只有當 commit、push 或其他受限制操作屬於使用者要求的成果時，才說明其授權不足或未執行情況。

未被要求的 commit、push 不屬於未完成事項，不得為此中止任務或主動要求授權。

## 完成條件

持續工作直到：

- 使用者要求的成果與明確要求的必要驗收均已完成，並完成目前環境允許且適合的其他驗證；或
- 所有剩餘必要工作皆受到本文件定義的真正阻塞影響。

只要仍有可安全推進的其他必要工作，單一子工作或單一驗證步驟受阻就不得作為停止整體任務的理由。

非必要操作受阻不影響任務完成。

所有剩餘必要工作均受阻時，應回報部分完成與阻塞狀態，不得宣稱整體任務已完成。

完成任務後，說明：

- 完成的主要修改。
- 執行過的驗證。
- 未執行的驗證與原因。
- 尚未解決且與任務有關的問題。
- 只有在使用者要求相關受限制操作時，才說明其未執行情況。
