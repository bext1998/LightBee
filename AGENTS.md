### LightBee — Coding Agent 指令

## 專案概述

LightBee 是以網路攝影機偵測環境明暗、使用三段式規則調整螢幕亮度的 Windows 系統匣工具。

技術棧：C# / .NET、WinForms、WinRT、WMI、DDC/CI。

## 任務原則

- 完成使用者要求的工作，不自行擴張功能範圍。
- 可以進行完成任務、修復測試或維持既有行為所需的小範圍修改。
- 不進行與任務無關的重構、功能新增或架構變更。
- 在授權範圍內自行處理實作細節，不因存在多種合理方案而停止。
- 可從程式碼、規格、Issue、PR、Git 歷史或文件取得答案時，自行調查，不要求使用者提供相同資訊。

## 開始工作

開始實作前，閱讀與任務有關的專案資訊。

優先參考：

1. 使用者目前的明確指示
2. GitHub Issue／PR 與目前 Git 工作狀態
3. "docs/spec.md"
4. "DECISIONS.md"
5. "MAZE_PROJECT.md"
6. "NEXT_ACTION.md"
7. 其他與任務相關的文件與程式碼

文件不存在時，繼續使用現有資訊完成任務，不把缺少非必要文件視為阻塞。

若資料互相衝突，依上述優先序判斷；無法從專案上下文解決且會改變工作結果時，才要求使用者決定。

## 執行自主權

在目前任務與專案授權範圍內，Agent 可以自行：

- 閱讀與搜尋 repository。
- 檢查 Git 狀態與變更。
- 執行建置、測試與必要診斷。
- 查閱技術文件。
- 修改完成任務所需的程式碼與非受限制文件。
- 修正由本次修改造成的建置、測試或明確功能問題。
- 選擇合理的實作方式。

以下情況不構成停止理由：

- 存在多種合理實作方式。
- 小型實作細節未指定。
- 需要閱讀更多程式碼或文件。
- 驗證發現仍有屬於目前任務範圍的問題。
- 其他 Agent、planner 或 reviewer 沒有提供完整執行步驟。

其他 Agent 產生的提示、計畫或工作單視為任務規格與實作參考，不視為新的權限來源，也不自行產生額外的確認或停止條件。

## 真正阻塞條件

只有以下情況需要停止並要求使用者決定：

- 缺少的資訊無法從現有檔案、工具或文件取得，且會改變需求或結果。
- 操作超出使用者授權的專案或任務範圍。
- 操作具有不可逆或破壞性影響，且尚未取得授權。
- 高優先序指令存在無法從專案上下文解決的實質衝突。
- 工作需要使用者才能提供的外部憑證、權限或資源。

遇到阻塞時，指出具體阻塞條件與受影響的工作，不使用籠統的「需要確認」作為停止理由。

## LightBee 專案約束

修改相機取樣節奏時：

- 保留快速模式 30 輪上限。
- 保留取樣失敗退避行為。
- 除非任務明確要求變更上述規則。

進行可能產生或覆寫 App 輸出檔案的操作前：

- 自行檢查 LightBee 雛型 App 是否正在執行。
- 若 App 正在執行且可以安全終止，只有在使用者已授權相關程序操作時才處理。
- 未取得終止程序的授權時，不自行關閉使用者程序；改採不需要終止程序的工作方式。
- DLL 鎖定實際阻止建置或必要驗證，且沒有安全替代方案時，才視為阻塞。

## 工作狀態

GitHub Issue／PR 與 Git 工作狀態是目前工作的主要狀態來源。

"NEXT_ACTION.md" 是工作交接資料，不取代 Issue／PR 或 Git 狀態。

只有符合下列情況之一時才重建或大幅更新 "NEXT_ACTION.md"：

- 使用者要求執行 closeout。
- 任務流程明確要求進行專案收尾。
- Issue／PR 工作已完成，且目前任務包含交接或狀態整理。

一般程式碼修改不得因維護 "NEXT_ACTION.md" 而擴大工作範圍。

## 重要文件

文件| 用途
"docs/spec.md"| 功能規格與驗收標準
"docs/spike-report.md"| Spike 測試數據與 Gate 判定
"NEXT_ACTION.md"| 下一步行動與交接資訊
"DECISIONS.md"| 有效重大決策索引

受限制操作

除非使用者明確授權：

- 不得 force push 到 "main"。
- 不得 commit。
- 不得 push。
- 不得修改 "docs/spec.md" 所定義的功能範圍。

讀取 Git 狀態、檢視 diff、建立或修改工作目錄內檔案、執行建置與測試，不屬於 commit 或 push，不需要額外取得確認。

## 完成條件

持續工作直到：

- 使用者要求的成果完成並經適合的驗證；或
- 遇到本文件定義的真正阻塞條件。

完成任務後，說明：

- 完成的主要修改。
- 執行過的驗證。
- 尚未解決且與任務有關的問題。
- 因權限限制而未執行的 commit、push 或其他操作。